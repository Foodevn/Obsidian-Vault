**Important - MUST READ**

In the next lecture, we'll create a new Angular project.

Make sure you add the `--strict false` and `--standalone false` flags when running `ng new` (e.g. run `ng new my-project --strict false --standalone false`) in the next lecture.

Thereafter, this tool will ask you **multiple questions and you can confirm the default options by pressing ENTER.**