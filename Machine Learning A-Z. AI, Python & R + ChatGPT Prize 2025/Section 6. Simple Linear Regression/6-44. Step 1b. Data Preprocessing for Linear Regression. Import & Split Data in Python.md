## Cấu trúc triển khai (Implementation Structure)

Quy trình xây dựng mô hình [[Hồi quy tuyến tính đơn (Simple Linear Regression)]] trong Python bao gồm các bước chính sau:
1. Nhập thư viện `(Importing libraries)`.
2. Nhập bộ dữ liệu `(Importing the dataset)`.
3. Chia dữ liệu thành tập huấn luyện và tập kiểm tra `(Splitting the dataset into Training set and Test set)`.
4. Huấn luyện mô hình trên tập huấn luyện.
5. Dự đoán kết quả trên tập kiểm tra.
6. Trực quan hóa kết quả tập huấn luyện.
7. Trực quan hóa kết quả tập kiểm tra.

> **Lưu ý**: Để học hiệu quả, hãy xóa các ô mã `(code cells)` có sẵn trong notebook mẫu và tự viết lại từ đầu (giữ lại các ô văn bản hướng dẫn).

---
# Tiền xử lý dữ liệu (Data Pre-processing)

## Tận dụng mẫu Code (Code Templates)
Thay vì viết lại từ đầu, ta có thể tái sử dụng mẫu code từ giai đoạn **Tiền xử lý dữ liệu** (Part 1). Quy trình này đảm bảo tính hiệu quả và chính xác.
## Các bước thực hiện

1. **Nhập thư viện**: Copy đoạn mã nhập `numpy`, `matplotlib`, và `pandas`.
2. **Nhập bộ dữ liệu**:
    - Copy đoạn mã nhập dữ liệu tiêu chuẩn.
    - **Thay đổi duy nhất**: Cập nhật tên file thành `Salary_Data.csv`.
    - Các lệnh `iloc` để lấy đặc trưng `(matrix of features)` và biến phụ thuộc `(dependent variable vector)` không cần sửa đổi vì chúng được viết tổng quát (lấy tất cả trừ cột cuối làm X, lấy cột cuối làm y).
3. **Chia tập dữ liệu**:
    - Copy đoạn mã sử dụng `train_test_split`.
    - Giữ nguyên tỷ lệ chia (thường là 80/20 hoặc 70/30 tùy bài toán).


```python
# Ví dụ về phần duy nhất cần chỉnh sửa trong template 
dataset = pd.read_csv('Salary_Data.csv') 
# Chỉ sửa tên file này 
X = dataset.iloc[:, :-1].values 
y = dataset.iloc[:, -1].values
```

## Thao tác trên Google Colab
Trước khi chạy code, cần đảm bảo dữ liệu đã được tải lên môi trường làm việc:
1. Nhấn vào biểu tượng thư mục (Files) bên trái.
2. Chọn nút **Upload**.
3. Tải lên file `Salary_Data.csv` từ thư mục máy tính (`Part 2 - Regression/Simple Linear Regression/Python`).

---
## Kết quả giai đoạn chuẩn bị

Sau khi chạy các bước trên, bạn đã hoàn thành giai đoạn tiền xử lý:
- Các thư viện cần thiết đã sẵn sàng.
- Dữ liệu lương và kinh nghiệm đã được nhập.
- Dữ liệu đã được chia tách thành **Tập huấn luyện** `(Training set)` và **Tập kiểm tra** `(Test set)`.

> **Bước tiếp theo**: [[Huấn luyện mô hình Hồi quy tuyến tính đơn]]